/**
 * DEPRECATED — use @/lib/auth instead.
 * This file is kept only for backward compatibility.
 * All auth config is now in app/api/auth/[...nextauth]/route.ts and lib/auth.ts
 */
export { authOptions as default } from "./auth";
